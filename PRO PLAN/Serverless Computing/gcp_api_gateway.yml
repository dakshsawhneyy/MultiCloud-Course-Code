swagger: '2.0'

info:
  title: hello-functions-api
  description: API Gateway with Cloud Functions
  version: 1.0.0

schemes:
  - https

produces:
  - application/json

paths:
  /hello:
    get:
      summary: Hello endpoint
      operationId: hello

      x-google-backend:
        address: https://YOUR_FUNCTION_URL

      responses:
        '200':
          description: Successful response
